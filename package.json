{
  "name": "iris",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run compile && npx tsc",
    "compile": "rimraf cpp/antlr4_grammar cpp/.antlr && antlr4 -Dlanguage=Cpp -no-listener -visitor ./cpp/iris_grammar.g4 -o ./cpp/antlr4_grammar && emcmake cmake -B build && cmake --build build -- -j && move-file build/wasm.js ts/wasm.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpy-cli": "^7.0.0",
    "move-file-cli": "^3.0.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  }
}
